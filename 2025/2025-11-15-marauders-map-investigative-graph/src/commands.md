## Common commands (Slice 0–2)

- Install dependencies:
  - `pip install -r requirements.txt`

- Run the dev server (Slice 0–2):
  - `python run.py`

- Run tests (including CRUD + search):
  - `pytest`



